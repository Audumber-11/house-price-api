services:
  - type: web
    name: house-price-api
    env: python
    buildCommand: pip install -r requirements.txt && python model_training.py
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"